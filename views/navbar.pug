nav.navbar.navbar-default.navbar-fixed-top
	.container-fluid
		.navbar-header
			button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#right-menu', aria-expanded='false')
				span.sr-only Toggle navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href='/') FairGrader @ GSW
		#right-menu.collapse.navbar-collapse
			ul.nav.navbar-nav.navbar-right
				if session.uid
					li.dropdown
						a.dropdown-toggle(href='#', data-toggle='dropdown', role='button', aria-haspopup='true', aria-expanded='false') #{session.name}
						ul.dropdown-menu
							li: a(href='/reports') Reports
							li.divider(role='separator')
							li: a(href='/logout') Logout
				else
					li: a(href='/login') Login
					li: a(href='/signup') Sign Up
.vgap
